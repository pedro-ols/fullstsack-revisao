<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pizzaria</title>
  </head>
  <body>
    <h2>Bem-vindo, <%= usuario.nome %>!</h2>
    <a href="/logout">Sair</a>

    <h3>🔍 Buscar Pizza</h3>
    <form method="GET">
      <input name="busca" placeholder="bucar por nome" value="<%= busca %>" />
      <button type="submit">Buscar</button>
    </form>

    <h3>🍕 Cadastrar pizza</h3>
    <form method="POST" action="/pizzas">
      <input name="name" placeholder="nome da pizza" required />
      <input
        name="preco"
        type="number"
        step="0.1"
        placeholder="preco"
        required
      />
      <input name="estoque" type="number" placeholder="estoque" value="10" />

      <button type="submit">Cadastrar</button>
    </form>

    <h3>📋 Pizzas</h3>
    <ul>
      <% pizzas.forEach(1 => {%>
      <li> 
        <strong> <% 1.nome %> </strong> - R$ <%= 1.preco %> (Estoque: <%=
        1.estoque %>) <% if(l.estoque < 5) { %> { %> <b>Estoque Baixo!</b> } <%
        } %>
        <form mthod="POST" action="/pizzas/update/<%= 1.id %>">
          <button type="submit">Excluir</button>
        </form>
      </li>
    </ul>
  </body>
</html>
